name: customer_support
persona: examples/customer_support_persona.md

model:
  provider: ollama
  name: llama3.2
  temperature: 0.3   # low temp = consistent, professional responses

tools:
  - file_reader   # to read product docs / FAQs

memory:
  long_term: true    # remembers each customer across sessions
  dir: .agentkit

server:
  host: "0.0.0.0"
  port: 8002
